{
  "name": "compvisproject",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix website",
    "build": "npm run build --prefix website",
    "preview": "npm run preview --prefix website"
  }
}
